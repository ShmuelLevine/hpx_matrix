#include <stdio.h>


int main(int argc, char *argv[])
{
    int pos = 33;
    int neg = -13;

    printf("%d / 8 equals: %d", pos, pos/8);
    printf("%d / 8 equals: %d", neg, neg/8);
    printf("%d % 8 equals: %d", pos, pos/8);
    printf("%d % 8 equals: %d", neg, neg/8);

    return 0;
}
